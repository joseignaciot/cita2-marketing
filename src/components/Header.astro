---
interface Props {
  activeNav?: string;
}

const { activeNav } = Astro.props;

const navItems = [
  { label: 'Producto', href: '/funcionalidades' },
  { label: 'Precios', href: '/precios' },
  { label: 'Sectores', href: '#', hasDropdown: true, dropdownItems: [
    { label: 'Peluquerías', href: '/peluquerias' },
    { label: 'Centros de estética', href: '/estetica' },
    { label: 'Dentistas', href: '/dentistas' },
    { label: 'Fisioterapia', href: '/fisioterapia' },
    { label: 'Tatuajes', href: '/tatuajes' },
    { label: 'Spas', href: '/spas' },
    { label: 'Restaurantes', href: '/restaurantes' },
    { label: 'Turismo', href: '/turismo' },
  ]},
  { label: 'Blog', href: '/blog' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-sm border-b border-gray-100">
  <nav class="max-w-7xl mx-auto container-padding">
    <div class="flex items-center justify-between h-16 md:h-20">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2">
        <div class="w-8 h-8 bg-primary-600 rounded-lg flex items-center justify-center">
          <span class="text-white font-bold text-lg">C</span>
        </div>
        <span class="text-xl font-bold text-gray-900">Cita2</span>
      </a>
      
      <!-- Desktop Nav -->
      <div class="hidden md:flex items-center gap-8">
        {navItems.map((item) => (
          <a 
            href={item.href}
            class={`text-sm font-medium transition-colors ${
              activeNav === item.href 
                ? 'text-primary-600' 
                : 'text-gray-600 hover:text-gray-900'
            }`}
          >
            {item.label}
          </a>
        ))}
      </div>
      
      <!-- CTA Buttons -->
      <div class="hidden md:flex items-center gap-4">
        <a 
          href="https://cita2.tecelia.com/login" 
          class="text-sm font-medium text-gray-600 hover:text-gray-900"
        >
          Iniciar sesión
        </a>
        <a 
          href="https://cita2.tecelia.com/register" 
          class="btn-primary text-sm"
        >
          Prueba gratis
        </a>
      </div>
      
      <!-- Mobile Menu Button -->
      <button 
        type="button"
        class="md:hidden p-2 text-gray-600 hover:text-gray-900"
        aria-label="Menu"
        onclick="document.getElementById('mobile-menu').classList.toggle('hidden')"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
    
    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden pb-4">
      <div class="flex flex-col gap-2">
        {navItems.map((item) => (
          <a 
            href={item.href}
            class="py-2 text-sm font-medium text-gray-600 hover:text-gray-900"
          >
            {item.label}
          </a>
        ))}
        <div class="flex flex-col gap-2 pt-4 border-t border-gray-100">
          <a 
            href="https://cita2.tecelia.com/login" 
            class="py-2 text-sm font-medium text-gray-600 hover:text-gray-900"
          >
            Iniciar sesión
          </a>
          <a 
            href="https://cita2.tecelia.com/register" 
            class="btn-primary text-center text-sm"
          >
            Prueba gratis 14 días
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<!-- Spacer for fixed header -->
<div class="h-16 md:h-20"></div>
